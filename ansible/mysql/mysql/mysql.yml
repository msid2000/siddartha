---
- name: MYSQL installtion on ubuntu
  hosts: all
  become: yes
  tasks:
    - name: install MYSQL
      ansible.builtin.apt:
        name: mysql
        update_cache: yes
        only_upgrade: yes
        state: present